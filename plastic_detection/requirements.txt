opencv-python>=4.8.0
numpy>=1.24.0
requests>=2.31.0
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# ── Hybrid detection pipeline (optional) ──
# Uncomment the following lines to enable the TACO Mask R-CNN +
# EfficientNet-B3 + TrashNet hybrid detection pipeline.
# These are OPTIONAL — the system will use YOLOv4 as fallback
# when PyTorch is not installed.
#
# torch>=2.0.0
# torchvision>=0.15.0
